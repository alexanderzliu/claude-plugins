[project]
name = "databricks-mcp-server"
version = "0.1.0"
description = "Databricks MCP server for Claude Code"
requires-python = ">=3.10"
dependencies = [
    "databricks-sdk>=0.20.0",
    "mcp>=1.0.0",
]

[project.scripts]
databricks-mcp-server = "src.main:main"
